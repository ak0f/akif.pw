/** Seconds from mount until the preloader curtain has fully cleared. */
export const PRELOADER_EXIT = 2.5;

/** When hero content begins its entrance, just as the curtain clears. */
export const HERO_ENTRANCE = PRELOADER_EXIT - 0.35;
